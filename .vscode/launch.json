{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "bun",
      "request": "launch",
      "name": "Debug Web API",
      "program": "${workspaceFolder}/webapi/src/index.ts", // Change this to your main file
      "cwd": "${workspaceFolder}/webapi"
    }
  ]
}

